.venv/
__pycache__/
*.pyc
.env
.vercel
.env*.local
.env*

# Large random blob read by the /blob route. Generated by gen_blob.sh during
# the Vercel build (see "buildCommand" in vercel.json), and locally for dev.
# Never committed (too big for GitHub's 100 MB file limit).
blob.bin
